# 🔢 Калькулятор: Интерактивный Арифметический Модуль

## 🌟 О проекте

Этот проект представляет собой функциональный **веб-калькулятор**, созданный для выполнения стандартных математических действий. Он разработан как практическое пособие для освоения основ **HTML, CSS и JavaScript**, а также для демонстрации современного дизайна в стиле **Neumorphism** с интерактивными анимациями.

### Визуализация проекта
<img width="557" height="672" alt="image" src="https://github.com/user-attachments/assets/a17ba2ed-f9c7-4877-96cb-4ff2a5448ca5" />

---

## 🛠️ Основной функционал

Калькулятор является полноценным инструментом для решения стандартных математических задач, дополненным современным интерфейсом и улучшенными функциями ввода.

### Базовые Арифметические Операции

Калькулятор выполняет следующие операции, которые обрабатываются функцией `calculate()`:

* **Сложение** (`+`)
* **Вычитание** (`-`)
* **Умножение** (`*`)
* **Деление** (`/`)

### Дополнительные возможности и особенности

| Возможность | Описание |
| :--- | :--- |
| **Проценты** (`%`) | Калькулятор может обрабатывать операцию взятия процента. Выражение преобразуется в десятичное значение (например, `50%` в `(50/100)`) для корректного расчета. |
| **Очистка дисплея** (`C`) | Функция `clearDisplay()` полностью сбрасывает текущее выражение, очищая поле ввода. |
| **Удаление символа** (`⌫`) | Функция `deleteLast()` позволяет удалить последний введенный символ, что удобно для исправления ошибок ввода. |
| **Обработка ошибок** | В случае некорректного выражения или попытки деления на ноль, калькулятор выводит на дисплей сообщение **"Ошибка"**. |
| **Замена оператора** | Функция `appendOperator()` заменяет старый оператор на новый, если они вводятся последовательно (например, `5++` будет исправлено на `5+`), предотвращая некорректные цепочки. |
| **Ввод с клавиатуры** | Калькулятор поддерживает ввод чисел и основных операторов (`+`, `-`, `*`, `/`) непосредственно с клавиатуры. Клавиши **Enter** или **равно (`=`)** выполняют вычисление, а **Backspace** удаляет символ. |

---

## 💻 Технологии

Проект реализован с использованием чистого **HTML, CSS и JavaScript** и не требует дополнительных библиотек или фреймворков.

## 🚀 Запуск проекта

Для запуска достаточно открыть файл `index.html` в любом современном веб-браузере.
